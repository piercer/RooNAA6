# Configure the ALSA loopback module
# enable=1  : enable the first loopback device
# index=2   : assign it to card index 2 (avoids conflicts with real sound cards)
options snd-aloop enable=1 index=2
